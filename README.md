# 🌍 EasyAI
---

## 🔹 Backend

Use this mode if you want to run the model interactively.

```bash
python train_model.py --interactive
```

Use this command to retrain the model from scratch.

```bash
python train_model.py --retrain
```
---